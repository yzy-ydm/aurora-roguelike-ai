# TASK-017.8_IMPLEMENTATION_REPORT — 怪物生成安全修复实施报告

> 日期：2026-09-24
> 状态：**实施完成，等待人工验收**
> 前置分析：[TASK-017.8_ANALYSIS_REPORT.md](TASK-017.8_ANALYSIS_REPORT.md)

---

## 1. 修改文件

| # | 文件 | 改动内容 |
|---|---|---|
| 1 | [world_coordinate.gd](client/scripts/world/world_coordinate.gd) | `monster_spawn_pos()` 增加可选参数 `platform_rects: Array[Rect2] = []`（**默认空数组，旧调用完全兼容**）；新增常量 `MONSTER_HALF_WIDTH=14`、`MONSTER_PLATFORM_CLEARANCE=4`、`MONSTER_GROUND_SCAN_X=550`；新增私有判定函数 `_monster_pos_clear()`；实现三级采样 |
| 2 | [room_spawner.gd](client/scripts/world/room_spawner.gd) | ① `spawn_monsters()` 调用点传入 `_get_platform_rects()`（1 行）② `spawn_boss()` 出生点从半空 (0,0) 改为平台感知地面安全点（2 行） |
| 3 | [test_monster_spawn_safety.gd](client/tests/test_monster_spawn_safety.gd) | **新增**测试（12 检查项 + 元护栏） |

**未修改**（遵守限制）：房间生成算法、平台生成算法、AI 系统、数据库、存档系统、战斗系统核心逻辑、奖励采样。

---

## 2. 修改原因

人工测试发现怪物出生在平台内部 → 子弹撞平台即销毁打不到 → `alive_monsters` 恒大于 0 → 无奖励无出口 → 战斗房永久软锁（P0）。

审计量化：战斗房平台 y 带 [272,308] 与怪物身体带 [268,296] 重叠 89%，单怪卡平台概率约 68%、房间内 2 只怪约 90%——几乎每场战斗房必然触发。

### 三级采样设计（y 恒为地面高度 282，怪物不出现在平台顶面，行为统一）

```
1) 槽位附近随机抖动（12 次，±30 内，钳制 ±550）
2) 地面带系统化扫描（-550 → +550，步进 56；
   起点按 index 错开 8px，使各怪扫描格点互不重合，防多怪拿到同一点）
3) 兜底：保留旧槽位点（地面带 1100px vs 平台总宽 ≤800px，理论不可达，防御性保留）
```

碰撞判定：怪物 28×28（半宽 14）+ 安全间距 4px − 浮点容差 0.1 → `rect.grow(17.9)` 不含出生点即为安全（与 TASK-002 奖励采样同一判定思路，复用现有 `_platform_rects` 基础设施，零新系统）。

Boss：旧逻辑出生在房间中心 (0,0) 半空，靠重力下落，落点可能被中央平台接住/卡住；现改为同一安全采样（出生在地面安全点）。

---

## 3. 测试结果

### 3.1 新增测试 test_monster_spawn_safety.gd：✅ 13/13 通过（12 检查项 + 元护栏）

| 测试 | 内容 | 结果 |
|---|---|---|
| T1 combat 房 | 真实 room_renderer 代码路径生成 3 平台矩形；2/3/4 怪各 20 轮采样（共 180 个出生点）全部避开平台且 y=282 | ✅ |
| T2 elite 房 | 真实 4 平台矩形；1/2 怪各 20 轮采样全部安全 | ✅ |
| T3 boss 房 | 真实 3 平台矩形；Boss 单怪 20 轮采样全部安全 | ✅ |
| T4 多怪不重叠 | total=4 同轮 4 点 20 轮，两两 x 互异（第二层扫描相位错开验证） | ✅ |
| T5 向后兼容 | 不传 platform_rects：total=1 x∈±[200,400]；total=3 x∈[-180,180]，y 恒 282——旧行为完全保持 | ✅ |

### 3.2 旧测试全量回归：✅ 零失败

| 测试套件 | 结果 |
|---|---|
| test_reward_system | 12/12 ✅ |
| test_reward_spawn_position | 11/11 ✅ |
| test_event_room | 16/16 ✅ |
| test_room_type_dispatch | 19/19 ✅ |
| test_room_lifecycle | 35/35 ✅ |
| test_floor_generation | 2001/2001 ✅ |
| test_monster_ai（TASK-017.7） | 14/14 ✅ |

### 3.3 其他验证

| 验证 | 结果 |
|---|---|
| Godot headless 启动（`--headless --quit`） | ✅ 零脚本错误 |
| 服务端 pytest（tests/ + ai/tests/） | ✅ 143 passed |

---

## 4. 剩余风险

| # | 风险 | 等级 | 说明 |
|---|---|---|---|
| 1 | 平台嵌入地面的视觉/碰撞问题（C-02） | P1 | 平台生成 y 带 [272,308] 中 py>288 的嵌进地面，形成地面矮墙。怪物出生点已避让，但玩家通行/跳跃体验仍受影响——属平台碰撞任务（下一顺序），本任务按限制未动 |
| 2 | 平台无单向碰撞（C-01） | P0 | 平台当墙用的问题仍存在，玩家从下方无法跳上——平台任务解决 |
| 3 | 怪物追击被平台阻挡 | P1 | 怪物无跳跃能力，追到平台边缘会停下打转（不构成软锁，玩家可击杀）——平台单向碰撞修复后怪物可穿过平台底部，一并缓解 |
| 4 | 采样判定容差 | 极低 | `_monster_pos_clear` 用 grow(17.9)（含 0.1 浮点容差），与测试判定一致；出生点距平台最小 4px+，物理解算不会把怪物挤进平台 |
| 5 | 清怪后奖励生成容器异常 | 极低 | render_room 保证容器存在；若异常则卡 REWARD——审计时已标记为观察项，不在本任务范围 |

**结论**：本次修复消除"怪物出生在平台内部"软锁根因（采样保证出生点与所有平台矩形保持 14+4px 安全距离，且有确定性扫描兜底），全部测试通过，无已知新引入风险。

---

**实施完成。按流程停止，等待人工验收（验收步骤见分析报告 §6.3：连续 5 个战斗房/精英房无怪物卡平台、无一场阻塞，Boss 房正常）。**
