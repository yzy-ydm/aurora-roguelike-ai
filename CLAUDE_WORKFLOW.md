# CLAUDE_WORKFLOW.md — 开发验证工作流

> 目的：减少无意义 token 消耗，同时保证代码质量。
> 适用范围：Claude Code 在本项目的所有开发任务。与 PROJECT_AUDIT_REPORT.md（现状审计）和 REFACTOR_PLAN.md（架构规划）配合使用。
> 本文件是**强制性规范**：每个任务开始前先判断等级，按对应验证要求执行；测试必须服务于任务目标，禁止无限测试。

---

## 1. 任务等级定义

### LEVEL-1：简单代码修改

| 项 | 内容 |
|---|---|
| 范围 | 变量修改、bug 修复（单文件/数行）、小逻辑调整、调试日志清理 |
| 示例 | 修正一个数值常量、修复空指针保护、删除调试 print |
| 必做验证 | ① 静态检查（grep 引用点/自检代码）② 单元测试（已有测试或新增最小测试） |
| 禁止 | 启动完整游戏、跑完整流程 |

### LEVEL-2：系统模块修改

| 项 | 内容 |
|---|---|
| 范围 | 单个系统的功能修改：奖励系统、怪物AI、存档、掉落、UI 面板等 |
| 示例 | 修复奖励出生点、接通 Boss 驱动、存档读档闭环 |
| 必做验证 | ① 代码检查（修改点 + 相关调用链 grep）② 相关测试（无头回归测试）③ 必要运行验证（仅验证本系统链路，如"奖励生成→拾取→信号"） |
| 禁止 | 为验证与本系统无关的玩法而绕路打完整局 |

### LEVEL-3：核心流程修改

| 项 | 内容 |
|---|---|
| 范围 | 地图系统、AI 生成系统、架构重构、坐标体系、存档模型变更等影响全局的任务 |
| 示例 | 房间图导航重构、AI 楼层生成恢复、坐标宪法落地 |
| 必做验证 | ① 全部静态检查 ② 全部相关测试 ③ **完整自动化游戏流程测试**（启动→登录→进游戏→核心循环→退出，以日志与断言为判定依据） |

**规则：只有 LEVEL-3 任务才需要完整自动化游戏测试。**

---

## 2. 验证规则总则

1. **测试服务于任务目标**。先问"这个测试验证的是本任务改了什么"，答不上来就不测。发现与任务无关的 bug 时：记录到报告"发现的其他问题"一节，不在本任务内修（除非 2 行内可修且不改变行为）。
2. **不无限测试**。每轮测试前明确"这轮要观察什么信号"；观察到目标信号即停止；连续 2 轮无进展必须停下来分析根因，而不是继续盲测。
3. **可复现优先**。能写成无头测试（tests/ 目录）的验证永远优先于人工开游戏；人工验收只负责"手感/视觉/体验"层面。
4. **静态检查先行**：修改完成后先 grep 自己的改动点与调用方，确认无遗漏再进入运行验证。
5. **真实验证必须有日志判定标准**：实机测试前先确定哪些日志行代表"通过"（例如 `[RoomComplete]`、`[Reward] Collected`）。

---

## 3. TASK_REPORT 规范

每个任务完成后必须输出 **TASK_REPORT.md**（覆盖式，一个任务一份；如任务有编号，文件名可用 `TASK-XXX_REPORT.md`）。

必须包含四节：

1. **修改内容** —— 改了哪些文件、每个文件改了什么（列表，不写废话）
2. **影响范围** —— 哪些系统/调用链受影响；是否影响存档、AI、地图等关联系统
3. **测试方式** —— 按任务等级写明：静态检查项 / 测试命令与结果 / 运行验证过程与日志证据
4. **测试结果** —— 通过/失败的明确结论；失败项必须写明原因与后续处理

可选追加：
- **发现的其他问题**（不修，仅记录）
- **下一步建议**

---

## 4. 人工验收规范

需要人工验收时，报告末尾必须写明三件事：

1. **操作步骤** —— 用户怎么操作（打开什么、点什么、按什么键），按 1、2、3 编号
2. **观察目标** —— 用户应该看到什么现象（画面/文字/音效层面）
3. **通过标准** —— 什么算通过、什么算不通过（例如"奖励物品出现在地面上且能拾取=通过；奖励飞到屏幕外=不通过"）

输出报告后**停止**，等待用户回复"验收通过"才能开始下一个任务。

---

## 5. 验证工具箱（已验证可用，直接复用）

### 5.1 无头启动检查（每个任务必做）

```bash
"/d/Godot_v4.7-stable_win64.exe/Godot_v4.7-stable_win64_console.exe" --headless --path "D:/GraduationProject/client" --quit
# 期望：零脚本错误，Autoload 全部初始化，LoginScene Ready
```

### 5.2 无头回归测试（tests/ 目录）

- 测试脚本写法：`extends SceneTree`，`_initialize()` 里搭桩 + 异步 `await physics_frame` 跑流程，`quit(1/0)` 退出。
- 现有测试：`client/tests/test_weapon_growth.gd`（需包装器）、`client/tests/test_reward_system.gd`（可直接运行）。
- 运行命令：

```bash
"/d/Godot_v4.7-stable_win64.exe/Godot_v4.7-stable_win64_console.exe" --headless --path "D:/GraduationProject/client" --script "res://tests/test_reward_system.gd"
```

### 5.3 服务端测试

```bash
cd /d/GraduationProject/server && ./venv/Scripts/python.exe -m pytest ai/tests/ -q   # 143 项
```

### 5.4 实机游戏测试（LEVEL-3 用）

环境事实（2026-09 验证，避免重复踩坑）：

- 游戏窗口标题：**"Aurora Roguelike AI (DEBUG)"**；窗口位置每次启动不同，用 `GetWindowRect` 动态换算（视口 1280x720 → 客户端像素线性映射）。
- 键盘注入**必须携带扫描码**（`MapVirtualKey`），否则 Godot 按物理键码匹配不到输入；登录文本框用**剪贴板粘贴**（Ctrl+A → Ctrl+V）绕过中文输入法。
- 辅助脚本位于 `C:\temp\gui\`：`shot.ps1`（截图）、`click.ps1`（鼠标点击）、`keys.ps1`（前台+按键）、`paste.ps1`（粘贴）、`hold.ps1`（按住键，带扫描码）、`login.ps1`（自适应坐标自动登录）、`winrect.ps1`（窗口矩形）。
- 升级面板（LevelUpPanel）弹出时游戏整体暂停（`get_tree().paused=true`），按钮位置在视口 (640,360) 起右下方，第一个选项约在视口 (640,425)。
- **碰撞层数值约定**（代码注释里的"第N层"表述与位值不一致，以数值为准）：Wall=1、Player=2、Enemy=4、PlayerBullet=8、EnemyBullet=16；掩码=各层数值之和。
- 测试账号：test001 / test123456（密码已重置）；后端服务启动：游戏服 `server/main.py` :8000，AI 服 `server/ai/main.py` :8001（依赖本机 MySQL）。
- 实机判定以**日志**为准（`/tmp/godot_game.log`）：关键日志如 `[RoomComplete]`、`[Portal]`、`[Reward] Collected`、`[RoomSpawner] All rewards collected!`。

### 5.5 已知问题速查（避免重复排查）

- 怪物 AI 静止（B-02）、Boss 无驱动（B-03）、读档无入口（B-04）、分支房间不可达+回溯软锁（B-05）——见 PROJECT_AUDIT_REPORT.md。
- 战斗房平台可能在地面高度挡路（B-09 的实机表现），实机测试被平台挡住时先确认平台位置再绕行。
- 升级面板出现时游戏暂停，点击未生效先检查日志里是否有 `[LevelUpPanel] Showing`。

---

## 6. 任务执行与停止规则

1. 接任务 → 判等级 → 按等级验证 → 输出 TASK_REPORT.md → **停止**。
2. 需人工验收的：按第 4 节格式写明操作步骤/观察目标/通过标准，等"验收通过"。
3. Git：每个重要阶段/任务完成且测试通过后提交并同步 GitHub；提交信息 `TASK-XXX:` 或 `Phase-X:` 开头；禁止提交未完成、未验证的代码（未完成工作保持在工作区并在报告中注明）。
4. 禁止：为了演示效果而跳过验证、用"应该没问题"代替测试、在报告中隐瞒失败项。
