# FINAL_SYSTEM_AUDIT_REPORT — 毕业设计最终工程审计

> 日期：2026-09-24
> 视角：本科毕业设计答辩（题目：《基于云端AI动态内容生成能力的2D横版Roguelike游戏系统设计与实现》）
> 状态：**仅审计，未修改任何代码**

---

## 1. 系统架构完整性检查

### 1.1 架构总览

```
┌─ Godot 4.7 客户端（1280×720 横版）────────────────────────────┐
│ 登录/主菜单/游戏场景 | 11 Autoload | 模块化脚本（~90 文件）      │
│ player/combat/enemy/world/ai/api/managers/models/ui            │
└──────────┬──────────────────────────────┬─────────────────────┘
    HTTP REST (JWT)                 HTTP REST (Bearer token)
           ▼                                 ▼
┌─ FastAPI 游戏服务 :8000 ─┐   ┌─ FastAPI AI 服务 :8001 ──────┐
│ auth/player/save/monster │   │ auth(内存token)/缓存/限流/     │
│ /weapon/map/event 路由   │   │ 质量校验/Agnes+MiMo双Provider  │
└──────────┬───────────────┘   └──────────────────────────────┘
           ▼
┌─ MySQL 8.0（aurora_game）────────────────────────────────────┐
│ users/player_profiles/weapons/player_weapons/monsters/maps/  │
│ events/game_saves（8 表，含 is_ai_generated 标记字段）        │
└─────────────────────────────────────────────────────────────┘
```

### 1.2 检查结论

| 层 | 结论 | 说明 |
|---|---|---|
| Godot 客户端 | ✅ 完整 | 场景流/输入/物理/战斗/UI/存档调用/AI 客户端全部可用，8 套自动化测试 2100+ 断言 |
| FastAPI 游戏服务 | ✅ 完整 | JWT 认证 + 8 组路由 + SQLAlchemy + 存档 PUT/GET/POST 语义（404/409 处理） |
| MySQL 数据库 | ✅ 完整 | 建表 SQL + 种子数据 + is_ai_generated 区分 AI 产物 |
| AI 服务 | ✅ 完整 | token 认证、缓存、限流、质量校验器、Agnes/MiMo 双 Provider + Factory、Prompt 构建器、怪物平衡服务 |
| HTTP 通信 | ✅ 完整 | 双服务独立认证体系；客户端 ApiClient 串行队列 + AI 独立 HTTPRequest（超时+401 自动恢复） |
| 数据流 | ✅ 完整 | 登录→profile→存档列表→资源→进游戏→AI 后台内容（异步不阻塞） |

### 1.3 设计缺陷观察（不影响运行，答辩需知晓）

| # | 缺陷 | 说明 |
|---|---|---|
| D1 | AI token 存内存，重启失效 | 已由客户端 401 自动重取兜底（TASK-020.2），属可接受的工程取舍（论文可写为"容错设计"） |
| D2 | 本地 SaveSystem 与服务端 SaveService 双轨 | 本地 JSON 存档是死代码（TASK-019 S-05），实际全走服务端 |
| D3 | 难度调整断链 | 见 §2（假功能） |
| D4 | BossController.update 零调用 | Boss 技能系统是死代码，Boss 当前由 MonsterAI 驱动 |
| D5 | 楼层拓扑由本地算法生成，AI 不参与 | TASK-006 冻结后的设计决策——论文须如实表述（"AI 增强内容，拓扑由确定性分层 DAG 算法保证可达性"） |

---

## 2. AI 功能真实性检查（逐项）

| # | AI 功能 | AI 输入产生处 | 请求发送处 | 返回解析处 | 最终影响游戏处 | 标记 |
|---|---|---|---|---|---|---|
| 1 | context_event | AIContextManager.get_event_context()（玩家等级/风格/血量） | AIContentService._call_cloud_ai_context_event | AIEventData.from_dict（Adapter 逐项验证） | 事件面板弹出，玩家选择→属性变化 | ✅ 完整闭环 |
| 2 | room_content | FloorManager 进房上下文（room_id/type/level） | _call_cloud_ai_room(_from_new) | AIParser.parse_room_content + validate_for_room_type | 怪物数量/类型/等级、奖励品质真实生效（finalize 防覆盖） | ✅ 完整闭环 |
| 3 | reward_generation | 随 room_content（rewards 段） | 同上 | RoomContentData.reward_items（TASK-022 类型安全） | RoomSpawner._generate_reward_from_strategy → RewardData 落地拾取 | ✅ 完整闭环 |
| 4 | upgrade_options | 玩家等级+属性快照 | _call_cloud_ai_upgrade | Adapter.to_dictionary_array → UpgradeData（本地池补足） | 升级面板 3 选 1 可选可生效 | ✅ 完整闭环 |
| 5 | difficulty_adjustment | 玩家行为统计（死亡率/承伤率/无伤率） | _call_cloud_ai_difficulty | _parse_difficulty_response（to_float 规整） | **仅调试面板显示，怪物生成/伤害计算零消费** | ❌ **假功能** |
| 6 | npc_dialogue | npc_type/环境/玩家状态 | _call_cloud_ai_dialogue | Adapter.to_string_array（崩溃已修） | **仅日志 print，无 UI 显示** | ⚠️ **部分实现** |

**结论：4 项完整闭环（context_event/room_content/reward/upgrade），1 项假功能（difficulty），1 项部分实现（dialogue）。** 答辩时如实表述；修复难度与生成部分零断链（见 §6）。

---

## 3. 游戏核心系统检查（十大系统）

| 系统 | 状态 | 检查发现 |
|---|---|---|
| 玩家系统 | ✅ | 移动/跳跃/冲刺/无敌/击退/死亡/复活完整；**玩家出生点 y=298 嵌入地面 18px（物理自动推出，观感瑕疵）** |
| 战斗系统 | ⚠️ | 状态机完整；**F-1 武器伤害双重计入**（get_attack 含武器 + bullet 再加 → 铁剑 5 打出 20 基础）；**F-4 双重减伤**（DamageSystem 公式 + PlayerStats 再减 defense） |
| 武器系统 | ⚠️ | 等级成长/冷却/发射完整；**F-5 crit_rate=0.1 硬编码**，暴击成长零消费者 |
| 怪物系统 | ✅ | AI 状态机（追/攻/前摇/保护期）、平台感知出生点、速度配置、掉落保护完整 |
| 奖励系统 | ✅ | 平台感知采样、策略映射、精确收集闭环 |
| 升级系统 | ✅ | EXP/3 选 1/属性生效/连续升级完整（暴击/移速加成接线属 TASK-007 遗留） |
| 地图系统 | ✅ | 分层 DAG 生成 + V1~V7 校验 + 紧急出口兜底（2001 项测试） |
| 房间系统 | ✅ | RoomState 生命周期状态机 + 唯一出口 + 类型分发 |
| Boss 系统 | ⚠️ | 出生/追击/死亡结算闭环 ✅；**BossController.update 零调用 → 技能/阶段转换是死代码**（D4） |
| 存档系统 | ⚠️ | 服务端保存链路已修复（TASK-020）；**F-6 槽位管理断裂**：_current_slot 恒 -1 → 暂停保存不可用、自动保存回退槽位 1、**无加载 UI 入口**、加载不恢复楼层 |

**其他发现**：死代码（save_system.gd 本地存档、monster_spawner.gd 旧生成器、room_spawner._apply_level_modifier 废弃函数）；无消费者变量（_difficulty_adjustment、PlayerStats.get_crit_rate/get_final_move_speed）；事件房自动选第一选项（简化设计）。

---

## 4. 毕业论文匹配检查

### 4.1 题目拆解与实现支撑

| 题目关键词 | 实现支撑 | 支撑度 |
|---|---|---|
| 云端 AI | 独立 AI 服务（:8001）、双 Provider（Agnes/MiMo）、token 认证、缓存、限流 | ✅ 强 |
| 动态内容生成 | 4 项真实闭环（事件/房间内容/奖励/强化）+ prompt 构建 + 质量校验 + 数据校验 | ✅ 强 |
| 2D 横版 | 1280×720 横版平台玩法（移动/跳跃/平台/单向待修） | ✅ 强 |
| Roguelike | 程序化楼层（分层 DAG）、房间类型多样、死亡重开、随机事件、成长选择、永久死亡 | ✅ 中强（有随机地图/死亡重置/成长选择；无局外成长，属轻 Roguelike——论文按 Roguelite 表述更严谨） |
| 系统设计与实现 | 三层架构、8 表数据库、状态机、算法、测试体系 | ✅ 强 |

### 4.2 论文可以写什么（真实且可答辩）

1. **三层分离架构**：游戏客户端 / 游戏服务（数据持久化）/ AI 内容服务（生成能力），REST + 双认证体系。
2. **AI 内容生成管道**：上下文采集（玩家行为/状态）→ Prompt 构建 → 云端调用 → **响应验证（类型安全适配层 + 质量校验器 + 规则校验器）** → 缓存 → 多级 fallback（服务不可用时本地生成，游戏仍可玩）——容错设计是加分点。
3. **AI 内容四类真实应用**：房间内容（怪物构成/奖励品质）、上下文事件（3 选 1 带风险回报）、强化选项（个性化 3 选 1）、NPC 对白/奖励策略（部分）。
4. **楼层生成算法（论文算法章节核心）**：分层 DAG 生成 + 7 项结构校验（数量/可达性/无死胡同/Boss 出度零/无环无回边）+ 失败重试；平台感知的怪物/奖励出生点三级采样算法。
5. **房间生命周期状态机**：ENTERING→COMBAT→REWARD→COMPLETED 唯一转换表 + 出口唯一性保证。
6. **战斗系统**：横版 AI 状态机（追击/攻击前摇/出生保护）、伤害公式、击退、无敌帧。
7. **数据库设计**：8 表 ER 图、JWT 用户体系、存档三槽位语义、is_ai_generated 区分 AI 产物。
8. **测试体系**：8 套无头自动化测试 2100+ 断言 + 服务端 pytest 143 项。
9. **工程过程**：审计驱动的迭代修复（可作为"系统测试与优化"章节素材）。

### 4.3 论文不能写什么（当前实现不支撑）

1. ❌ **"动态难度调整实时生效"**——difficulty 生成后零消费者（除非先完成 F-2 修复）。
2. ❌ **"AI 生成楼层拓扑"**——楼层结构由本地算法生成（TASK-006 冻结后），论文需如实写"AI 增强房间内容层"。
3. ❌ **"AI 对白展示系统"**——dialogue 无 UI 消费（除非先完成 F-3）。
4. ❌ **"完整的存档读档"**——加载入口缺失（除非先完成 F-6）。
5. ❌ **Boss 多阶段技能系统**——BossController 技能是死代码，论文应只写 Boss 的基础战斗与结算流程。
6. ❌ 数值平衡结论（暴击成长/移速成长未接线、伤害公式存在双计，在修复前不宜给出平衡数据）。

---

## 5. 答辩风险检查（模拟老师提问 36 问）

### A. 为什么使用 AI / AI 价值（6 问）

1. 为什么 Roguelike 需要 AI？→ 传统做法是策划手工配置内容表；本系统将"玩家行为上下文"输入云端大模型，产出个性化内容（事件/强化/房间构成），实现"千人千面"的重玩价值。
2. AI 在哪里体现？→ 独立 AI 服务（8001），客户端经 AI Content Service 异步请求；生成 4 类真实生效内容（见 §2 表）。
3. AI 生成是否真实调用云端大模型？→ 是：Agnes/MiMo 双 Provider + Factory 切换，token 认证；日志可证明 HTTP 200 响应（备好演示）。
4. AI 生成的东西是"摆设"还是真影响游戏？→ 4 项真实闭环（进房事件可选、房间怪物构成、奖励类型数值、升级选项）；1 项（难度）为生成后存储未消费——**如实回答**。
5. AI 服务挂了游戏还能玩吗？→ 能。多级 fallback：本地 mock 生成 + 本地楼层算法，游戏完全可玩（容错设计）。
6. AI 生成的内容怎么保证合法？→ 三层防线：响应解析类型安全（Adapter 逐项验证）→ 规则校验（房间类型约束，如事件房禁止怪物）→ 质量校验器打分（低于阈值走 fallback）。

### B. 动态难度 / AI 细节（6 问）

7. 动态难度如何实现？→ 当前：采集玩家行为指标（死亡/承伤/无伤率）→ AI 生成 multiplier → 存储；**消费端未接线**（如实说明，或完成 F-2 后回答完整链路）。
8. Token 过期怎么处理？→ 服务端 24h 过期+内存存储；客户端 401 自动重取并重试一次（容错设计）。
9. AI 响应超时/乱格式怎么办？→ HTTP 超时（5s/10s）→ 空响应 → fallback；格式异常经 Adapter 类型验证过滤非法项。
10. 提示词怎么设计？→ PromptBuilder 服务端组装（角色设定+上下文数据+输出 JSON schema 约束）。
11. AI 生成的数据会持久化吗？→ 数据库 monsters/events/weapons 表含 is_ai_generated 标记字段，AI 产物可入库。
12. 缓存机制？→ AI 服务内存缓存 + 客户端缓存管理器。

### C. Roguelike 机制（5 问）

13. 随机性从哪里来？→ 楼层 DAG 随机（层数/每层房数/边）、房间类型概率（按楼层分布）、怪物数值区间、平台布局、奖励随机。
14. 地图怎么保证不卡关？→ 分层 DAG 保证前向出度≥1 + BFS 可达性校验 + 失败重试 + 紧急出口兜底（绝不软锁）。
15. 死亡惩罚？→ 死亡即 GameOver，重新开始（perma-death）；无局外成长（如实说明是轻 Roguelike）。
16. 房间类型有哪些？→ START/COMBAT/ELITE/REWARD/EVENT/TREASURE/SHOP/BOSS 八类，各自独立流程。
17. 成长系统？→ 战斗掉 EXP→升级（生命/攻击自动成长）+ 3 选 1 强化（AI 优先）+ 房间奖励拾取 + 武器升级。

### D. 算法与工程量（6 问）

18. 算法在哪里？→ ①楼层分层 DAG 生成+校验；②平台感知出生点三级采样；③房间状态机转换表；④伤害公式与防御减伤；⑤怪物 AI 状态机（追击/前摇/保护）。
19. 你自己的代码量？→ 客户端 ~90 个 GDScript 文件、服务端 ~70 个 Python 文件、8 套测试（可现场展示文件树与 git 历史）。
20. 测试怎么做的？→ Godot 无头模式 SceneTree 测试框架 + 元护栏；服务端 pytest；每个任务全量回归。
21. 数据库表关系？→ users 1:N player_profiles/weapons/player_weapons/game_saves；独立 monsters/maps/events 内容表。
22. 为什么拆两个服务？→ 职责分离：游戏服务管数据与进度（需稳定低延迟）；AI 服务管生成（大模型调用慢且贵，独立可扩展/可降级）。
23. 异步怎么处理？→ 客户端 AI 请求全部 await 协程不阻塞游戏；ApiClient 串行队列防并发冲突。

### E. 系统设计（7 问）

24. 存档怎么设计？→ 三槽位、服务端持久化、PUT 更新/404 自动转 POST 创建；玩家状态 JSON 快照。
25. 战斗伤害公式？→（修复后）基础=(攻击+武器伤害)×(1-防御/(防御+100))，暴击×1.5，最低 1 点。**当前存在武器双计与双重减伤——修复前如实说明**。
26. 怪物 AI 状态机？→ IDLE/CHASE/ATTACK(+前摇)/DEAD；出生保护 0.8s；受击硬直。
27. 为什么出生保护/前摇？→ 开局体验审计后加入：防贴脸即伤、给玩家反应窗口（设计迭代案例，答辩可讲）。
28. 平台碰撞为什么用单向？→ 单向碰撞允许从下跳上、从上落地，是横版标准方案（当前待修，修复前如实说明）。
29. 事件房流程？→ 进房→EVENT 状态→AI 事件面板→选择→应用奖励/风险→完成→出口。
30. Boss 流程？→ 独立 Boss 房→战斗→结算（技能系统未启用，如实说明）。

### F. 数据与真实性（6 问）

31. 游戏里的怪物是 AI 生成的吗？→ 怪物基础库来自数据库/资源服务；AI 生成的是房间内容中的怪物构成（数量/类型/等级）；AI 生成的怪物示例数据有 is_ai_generated=1 标记。
32. 怎么证明 AI 生成是"动态"的？→ 输入为实时玩家上下文（等级/风格/血量/行为统计），不同上下文产出不同内容（演示两次不同状态进房）。
33. 大模型调用成本/延迟怎么处理？→ 后台异步（不阻塞）+ 缓存 + fallback；演示可用 mock 对比。
34. 项目怎么管理迭代？→ 任务制（TASK-001~024）每任务：审计→实施→测试→人工验收→报告（可展示任务报告文档树）。
35. 版本管理？→ git 提交历史 + GitHub 仓库（**注意：当前工作区有 TASK-017.7 以来的未提交改动，须提交**）。
36. 项目还有什么不足？→ 主动回答：难度消费端未接、存档加载入口待补、Boss 技能未启用、数值平衡未完成——展示对系统缺陷的清醒认识（比隐瞒更有说服力）。

---

## 6. 最终开发路线

### P0 必须完成（不完成则答辩演示有硬伤）

| # | 项 | 原因 | 影响 | 预计修改文件 | 风险 |
|---|---|---|---|---|---|
| P0-1 | **git 全量提交 + GitHub 同步** | 工作区自 TASK-017.7 起全部代码改动未提交（含 24 份任务报告）；答辩前必须入库 | 无版本保护、仓库与本地脱节 | git 操作 | 低（先检查再完整提交，禁止部分提交） |
| P0-2 | **存档加载入口 + 槽位管理（F-6）** | 答辩"存档/读档"演示是必问题；当前无加载入口、暂停保存不可用 | 演示失败风险最高 | main_scene.gd（继续游戏按钮）、game_flow_controller.gd、game_scene.gd、save_selection.gd | 低~中（涉及场景切换流程，改后全量回归） |
| P0-3 | **难度调整消费点（F-2）** | "动态难度"是论文核心卖点，当前是假功能 | 若被问"难度怎么生效"将无法回答 | room_spawner.gd（怪物 clamp 读 multiplier） | 低（~6 行，需钳制倍率防失控） |
| P0-4 | **NPC 对白 HUD 显示（F-3）** | AI 叙事零可见，AI 生成能力展示缺失一块 | 弱化 AI 丰富度 | floor_manager.gd（信号）+ game_scene.gd/hud（显示） | 低（最小 HUD 方案） |
| P0-5 | **答辩文档六件套**（阶段六） | ARCHITECTURE/SYSTEM_DESIGN/AI_DESIGN/ALGORITHM_DESIGN/DATABASE_DESIGN/TEST_REPORT | 论文主体素材 | docs/ 新文档 | 低 |

### P1 建议完成（提升可信度与数值诚实性）

| # | 项 | 原因 | 预计修改文件 | 风险 |
|---|---|---|---|---|
| P1-1 | 武器伤害双计修复（F-1）+ 双重减伤（F-4） | 数值体系失真；被问公式时自相矛盾 | damage_system.gd、player_stats.gd | 中（玩家 DPS -25%，需实测微调怪物数值） |
| P1-2 | 暴击率接线（F-5） | 成长系统"假选项" | weapon.gd（1 行） | 低 |
| P1-3 | API Key 轮换 | 真实 Key 已泄漏到公开仓库 | 用户账号操作（Claude 无法代办） | 无 |
| P1-4 | BossController.update 接入（或论文明确不写 Boss 技能） | 死代码若被问及 | boss_controller.gd + monster_node 驱动 | 中 |

### P2 可选优化（时间允许时做）

| # | 项 | 原因 | 预计修改文件 | 风险 |
|---|---|---|---|---|
| P2-1 | 死代码清理（save_system.gd/monster_spawner.gd/_apply_level_modifier） | 代码整洁度 | 删除/标记废弃 | 低（需确认无引用） |
| P2-2 | 事件选择 UI（当前自动选第一项） | 事件房体验完整 | game_scene 事件流程 + 复用 LevelUpPanel 模式 | 中 |
| P2-3 | 平台单向碰撞 + 嵌地修正（C-01/C-02） | 横版手感核心问题 | room_renderer.gd | 低~中 |
| P2-4 | 玩家出生点对齐（P-01） | 观感 | world_coordinate.gd（1 行） | 极低 |
| P2-5 | TASK-007 奖励选择 | 成长闭环强化 | 见 TASK-007_ANALYSIS_REPORT | 中（范围较大，时间紧可不做） |

---

## 7. 总结论

**系统层面**：三层架构成立、十条核心系统闭环（Boss 技能与存档加载除外）、AI 内容生成 4/6 真实闭环、自动化测试体系完整——**已达到毕业设计答辩的基本盘**。

**最关键的 5 件事**（按顺序）：① 立即 git 提交保护成果；② 补存档加载入口（演示硬伤）；③ 接难度消费点（论文卖点）；④ 对白可见（AI 丰富度）；⑤ 论文六件套文档。随后按时间允许完成 P1/P2。

**答辩策略建议**：如实表述系统边界（哪些做了、哪些是简化设计、哪些未完成），主动说明"难度消费端/存档加载/Boss 技能"三个已知短板及修复计划——展示工程判断力比隐藏缺陷更得分。

**审计完成，未修改任何代码。等待下一步指令。**
