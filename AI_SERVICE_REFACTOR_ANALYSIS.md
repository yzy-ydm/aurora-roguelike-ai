# AIService Provider 解耦分析

> 生成日期：2026-09-22
> 目的：识别并移除 ai_service.py 中对 MiMo 的硬编码依赖

---

## 一、当前 Provider 耦合点汇总

### 1.1 Import 耦合（1处）

| 行号 | 代码 | 问题 |
|------|------|------|
| 18 | `from .mimo_client import MimoClient` | 直接导入具体Provider类 |

### 1.2 文档字符串耦合（3处）

| 行号 | 代码 | 问题 |
|------|------|------|
| 33 | `- mimo: 调用小米MiMo API` | 文档绑定MiMo |
| 36 | `- LLM_PROVIDER: mock / mimo` | 环境变量描述不完整 |
| 37 | `- MIMO_API_KEY: MiMo API Key` | 只提及MiMo配置 |

### 1.3 初始化逻辑耦合（11处）

| 行号 | 代码 | 问题 |
|------|------|------|
| 50 | `os.getenv("LLM_PROVIDER", "mock")` | 只读旧变量名 |
| 60 | `if self.provider_type == "mimo":` | 硬编码分支判断 |
| 61 | `self.llm_provider = MimoClient()` | 直接实例化 |
| 65 | `logger.info(f"AIService initialized with MiMo provider")` | 日志绑定MiMo |
| 66 | `logger.info(f"MiMo config: ...")` | 日志绑定MiMo |
| 68 | `logger.warning("AIService: MiMo provider not available...")` | 日志绑定MiMo |
| 70 | `logger.info("AIService initialized with Mock mode")` | Mock模式日志 |

### 1.4 ai_mode 字段硬编码（11处）

| 行号 | 代码 | 问题 |
|------|------|------|
| 108 | `result["ai_mode"] = "mimo"` | 硬编码字符串 |
| 123 | `result["ai_mode"] = "mimo"` | 同上 |
| 181 | `result["ai_mode"] = "mimo"` | 同上 |
| 196 | `result["ai_mode"] = "mimo"` | 同上 |
| 254 | `result["ai_mode"] = "mimo"` | 同上 |
| 269 | `result["ai_mode"] = "mimo"` | 同上 |
| 325 | `result["ai_mode"] = "mimo"` | 同上 |
| 340 | `result["ai_mode"] = "mimo"` | 同上 |
| 828 | `result["ai_mode"] = "mimo"` | 同上 |
| 864 | `result["ai_mode"] = "mimo"` | 同上 |
| 1003 | `result["ai_mode"] = "mimo"` | 同上 |
| 1122 | `result["ai_mode"] = "mimo"` | 同上 |
| 1214 | `result["ai_mode"] = "mimo"` | 同上 |
| 1301 | `result["ai_mode"] = "mimo"` | 同上 |
| 1386 | `result["ai_mode"] = "mimo"` | 同上 |

### 1.5 Fallback 逻辑硬编码（7处）

| 行号 | 代码 | 问题 |
|------|------|------|
| 135 | `"mock_fallback" if self.provider_type == "mimo" else "mock"` | 绑定MiMo |
| 208 | 同上 | 同上 |
| 281 | 同上 | 同上 |
| 352 | 同上 | 同上 |
| 836 | 同上 | 同上 |
| 872 | 同上 | 同上 |
| 1011 | 同上 | 同上 |
| 1130 | 同上 | 同上 |
| 1222 | 同上 | 同上 |
| 1309 | 同上 | 同上 |
| 1394 | 同上 | 同上 |

### 1.6 _should_use_llm() 耦合（1处）

| 行号 | 代码 | 问题 |
|------|------|------|
| 365 | `self.provider_type == "mimo"` | 绑定MiMo |

### 1.7 日志信息硬编码（多处）

| 示例 | 问题 |
|------|------|
| `logger.info(f"Floor generated by MiMo: ...")` | 绑定MiMo |
| `logger.warning(f"MiMo generation failed, retrying once: ...")` | 绑定MiMo |
| `logger.warning(f"MiMo retry failed, fallback to mock: ...")` | 绑定MiMo |
| （共14处类似日志） | |

---

## 二、修改方案

### 核心原则

1. **不删除现有代码** — 保留所有业务逻辑
2. **只替换Provider相关代码** — 不改generate_*方法体
3. **使用动态名称** — `provider.get_provider_name()` 替代硬编码字符串
4. **工厂模式** — 通过ProviderFactory创建实例

### 修改清单

#### 修改1：Import 替换（第18行）

```python
# 修改前
from .mimo_client import MimoClient

# 修改后
from .provider_factory import ProviderFactory
```

#### 修改2：初始化逻辑（第50-70行）

```python
# 修改前
self.provider_type = os.getenv("LLM_PROVIDER", "mock").lower()
...
if self.provider_type == "mimo":
    self.llm_provider = MimoClient()
    ...

# 修改后
self.provider_type = os.getenv("AI_PROVIDER", os.getenv("LLM_PROVIDER", "mock")).lower()
...
if self.provider_type != "mock":
    self.llm_provider = ProviderFactory.create(self.provider_type)
    if self.llm_provider and self.llm_provider.is_available():
        self.prompt_builder = PromptBuilder()
        logger.info(f"AIService initialized with {self.llm_provider.get_provider_name()} provider")
    else:
        logger.warning(f"AIService: {self.provider_type} not available, will fallback to mock")
else:
    logger.info("AIService initialized with Mock mode")
```

#### 修改3：ai_mode 动态化（所有 "mimo" 替换）

```python
# 修改前
result["ai_mode"] = "mimo"

# 修改后
result["ai_mode"] = self.llm_provider.get_provider_name() if self.llm_provider else "mock"
```

#### 修改4：Fallback 逻辑泛化（所有 fallback 替换）

```python
# 修改前
result["ai_mode"] = "mock_fallback" if self.provider_type == "mimo" else "mock"

# 修改后
result["ai_mode"] = "mock_fallback" if self.llm_provider is not None else "mock"
```

或者更简洁：
```python
# 使用 helper 方法
result["ai_mode"] = self._get_ai_mode()
```

其中：
```python
def _get_ai_mode(self) -> str:
    if self.llm_provider:
        return self.llm_provider.get_provider_name()
    return "mock"
```

#### 修改5：_should_use_llm() 重构（第362-368行）

```python
# 修改前
def _should_use_llm(self) -> bool:
    return (
        self.provider_type == "mimo"
        and self.llm_provider is not None
        and self.llm_provider.is_available()
        and self.prompt_builder is not None
    )

# 修改后
def _should_use_llm(self) -> bool:
    return (
        self.llm_provider is not None
        and self.llm_provider.is_available()
        and self.prompt_builder is not None
    )
```

#### 修改6：日志动态化

```python
# 修改前
logger.info(f"Floor generated by MiMo: {result.get('room_count', 0)} rooms")
logger.warning(f"MiMo generation failed, retrying once: {e}")

# 修改后
provider_name = self.llm_provider.get_provider_name() if self.llm_provider else "Unknown"
logger.info(f"Floor generated by {provider_name.capitalize()}: {result.get('room_count', 0)} rooms")
logger.warning(f"{provider_name.capitalize()} generation failed, retrying once: {e}")
```

---

## 三、影响范围评估

### 3.1 受影响文件

| 文件 | 修改量 | 风险等级 |
|------|--------|----------|
| `ai_service.py` | ~66行替换 | 🟡 中 |
| `provider_factory.py` | 新增（已完成） | 🟢 低 |
| `test_ai_service.py` | 可能需更新 | 🟢 低 |
| `test_agnes_provider.py` | 无需修改 | 🟢 无 |

### 3.2 不受影响

| 项目 | 原因 |
|------|------|
| Godot客户端 | 不解析ai_mode字段值 |
| 业务逻辑 | generate_*方法体不变 |
| 缓存机制 | cache key不变 |
| 验证器/质量检查器 | 与Provider无关 |
| PromptBuilder | 独立于Provider |

### 3.3 测试兼容性

**需要验证：**
- 现有13个ai_service测试是否通过
- 新增Provider Factory测试
- 新增AIService Provider切换测试

**预期结果：** 全部通过（94+新测试）

---

## 四、向后兼容性保证

| 场景 | 行为 |
|------|------|
| `LLM_PROVIDER=mimo` | 仍然工作（兼容旧配置） |
| `LLM_PROVIDER=mock` | 仍然工作 |
| `AI_PROVIDER=agnes` | 新增支持 |
| `AI_PROVIDER=mimo` | 新增支持 |
| `AI_PROVIDER=mock` | 新增支持 |

---

## 五、执行顺序

```
Step 1: 修改 ai_service.py (init + should_use_llm + _get_ai_mode helper)
Step 2: 替换所有 "mimo" 字符串为动态获取
Step 3: 运行现有测试 (预期94个通过)
Step 4: 编写新测试 (ProviderFactory + Provider Switching)
Step 5: 运行全部测试 (预期 >94 个通过)
Step 6: 更新文档和CHANGELOG
Step 7: Git commit + push
```

---

*分析完成，等待确认后开始编码。*
