# AI内容质量保障体系设计

> 文档版本：v1.0
> 更新日期：2026-09-22
> 适用项目：Aurora-Roguelike-AI

---

## 一、为什么需要AI内容验证

### 1.1 问题背景

在Roguelike游戏开发中，使用AI生成游戏内容已成为趋势。然而，AI生成存在以下固有风险：

| 风险类型 | 具体表现 | 游戏影响 |
|----------|----------|----------|
| **数值越界** | HP=99999, Attack=9999 | 怪物过于强大，玩家无法通关 |
| **格式错误** | JSON解析失败、字段缺失 | 游戏崩溃或显示异常 |
| **平衡性破坏** | 奖励过强/过弱 | 游戏经济系统失衡 |
| **逻辑冲突** | 房间类型与内容不匹配 | 游戏流程异常 |

### 1.2 验证必要性

```
AI生成内容
    ↓
┌─────────────────────┐
│  质量保障体系        │
│  ├─ 格式验证        │
│  ├─ 范围验证        │
│  ├─ 平衡性检查      │
│  └─ 质量评分        │
└─────────────────────┘
    ↓
进入游戏
```

**核心目标：** 确保AI生成的内容安全、可用、平衡。

---

## 二、验证流程设计

### 2.1 完整流水线

```
AI Provider.generate(prompt)
    ↓
解析JSON (JSON容错解析)
    ↓
┌──────────────────────────────────────────────────────────┐
│                   第一层：格式验证                        │
│  AIValidator.validate_*()                                │
│  ├─ 检查必需字段                                        │
│  ├─ 检查数据类型                                        │
│  ├─ 枚举值验证 (房间类型/稀有度)                          │
│  └─ 默认值修复                                          │
└──────────────────────────────────────────────────────────┘
    ↓
┌──────────────────────────────────────────────────────────┐
│                   第二层：范围验证                        │
│  AIValidator._validate_monster_attributes()              │
│  ├─ HP: 10-500 (修复过高/过低)                           │
│  ├─ Attack: 1-50 (修复过高)                              │
│  └─ Defense: 0-30 (修复过高)                             │
└──────────────────────────────────────────────────────────┘
    ↓
┌──────────────────────────────────────────────────────────┐
│                   第三层：质量检查                        │
│  AIQualityChecker.check_*()                              │
│  ├─ 数值裁剪                                            │
│  ├─ 枚举值修正                                          │
│  └─ 结构完整性修复                                       │
└──────────────────────────────────────────────────────────┘
    ↓
┌──────────────────────────────────────────────────────────┐
│                   第四层：质量评分                        │
│  AIQualityChecker.calculate_quality_score()              │
│  ├─ legality (30%): 格式正确性                           │
│  ├─ balance (30%): 数值平衡性                            │
│  ├─ diversity (20%): 内容多样性                          │
│  └─ completeness (20%): 字段完整性                       │
└──────────────────────────────────────────────────────────┘
    ↓
评分 < 阈值? → Fallback到Mock
    ↓
缓存 + 返回
```

### 2.2 各层级职责

| 层级 | 组件 | 职责 | 输出 |
|------|------|------|------|
| L1 | AIValidator | 结构校验、字段修复 | 修复后的数据 |
| L2 | AIValidator | 数值范围验证 | 裁剪后的数值 |
| L3 | AIQualityChecker | 质量检查、平衡验证 | 修正后的数据 |
| L4 | AIQualityChecker | 质量评分 | 0-100分 |

---

## 三、数值范围设计

### 3.1 怪物属性范围

| 属性 | 最小值 | 最大值 | 设计依据 |
|------|--------|--------|----------|
| HP | 10 | 500 | 1级玩家约50-100HP，Boss不超过500 |
| Attack | 1 | 50 | 玩家攻击约10-30，怪物不应过高 |
| Defense | 0 | 30 | 玩家防御约5-15，怪物合理上限 |

### 3.2 武器属性范围

| 属性 | 最小值 | 最大值 | 设计依据 |
|------|--------|--------|----------|
| Damage | 1 | 999 | 基础伤害1-10，传说级武器上限999 |
| Rarity | common | legendary | 5级稀有度系统 |

### 3.3 房间配置范围

| 属性 | 最小值 | 最大值 | 设计依据 |
|------|--------|--------|----------|
| Difficulty | 1 | 10 | 标准难度等级 |
| Monster Count | 1 | 10 | 单房间怪物上限 |
| Reward Quality | 0.1 | 3.0 | 奖励倍率范围 |

---

## 四、质量评分机制

### 4.1 四维度评分模型

```
总质量分 = Σ(维度得分 × 权重)

其中：
- legality (合法性): 30%
  - 字段完整性: 25分
  - 类型正确性: 25分
  
- balance (平衡性): 30%
  - 数值范围合规: 100分基准
  - 每越界一项扣20分
  
- diversity (多样性): 20%
  - 房间类型分布: +30分
  - 怪物种类差异: +30分
  
- completeness (完整性): 20%
  - 必需字段齐全: 按比例计分
```

### 4.2 评分示例

| 场景 | 评分结果 | 说明 |
|------|----------|------|
| 完美生成 | 95/100 | 字段完整、数值合理、多样性好 |
| 轻微越界 | 75/100 | 部分数值超限，已自动修复 |
| 严重问题 | 45/100 | 多处异常，需降级到Mock |
| 完全失败 | 0/100 | 格式错误，必须降级 |

### 4.3 降级决策

```python
QUALITY_THRESHOLD = 50  # 质量分阈值

if quality_score < QUALITY_THRESHOLD:
    logger.warning(f"Low quality score: {quality_score}, falling back to Mock")
    return mock_generate()
else:
    return ai_generated
```

---

## 五、实现架构

### 5.1 核心组件

```
server/ai/services/
├── ai_validator.py          # 格式验证器 (已增强)
├── ai_quality_checker.py    # 质量检查器 (已增强)
└── provider_factory.py      # Provider工厂
```

### 5.2 验证规则配置

```python
# AIValidator常量
MONSTER_HP_MIN = 10
MONSTER_HP_MAX = 500
MONSTER_ATTACK_MAX = 50
MONSTER_DEFENSE_MAX = 30
VALID_ROOM_TYPES = {"start", "combat", ...}
VALID_RARITIES = {"common", "uncommon", ...}

# AIQualityChecker常量
MIN_HEALTH = 10
MAX_HEALTH = 500
DIMENSION_WEIGHTS = {
    "legality": 0.30,
    "balance": 0.30,
    "diversity": 0.20,
    "completeness": 0.20
}
```

---

## 六、测试覆盖

### 6.1 单元测试

| 测试文件 | 测试数 | 覆盖内容 |
|----------|--------|----------|
| test_validator.py | 14 | 格式验证、类型检查、默认值修复 |
| test_quality_checker.py | 13 | 质量检查、数值裁剪、评分计算 |
| test_content_validator.py | 15 | 新增的数值范围验证、枚举验证 |

### 6.2 测试场景

```python
# 测试HP越界
monster = {"health": 99999}
result = validator._validate_monster_attributes(monster)
assert result["health"] == 500  # 被裁剪到上限

# 测试非法房间类型
assert validator.validate_room_type("invalid") == "combat"

# 测试质量评分
score = checker.calculate_quality_score(data, "floor")
assert 0 <= score["quality_score"] <= 100
```

---

## 七、论文写作要点

### 7.1 可阐述的技术点

1. **多层验证架构** — 展示软件工程的分层设计思想
2. **数值范围约束** — 体现游戏平衡性设计能力
3. **质量评分模型** — 展示算法设计与量化评估能力
4. **Fallback机制** — 体现系统鲁棒性设计

### 7.2 可引用的数据

- 验证通过率：XX%（可通过日志统计）
- 平均质量分：XX/100
- Mock降级率：XX%（AI失败时的回退比例）

---

## 八、后续优化方向

1. **动态范围调整** — 根据玩家等级动态调整验证阈值
2. **历史对比** — 与历史生成记录对比，避免重复
3. **用户反馈** — 收集玩家对生成内容的评分
4. **A/B测试** — 对比不同验证策略的效果

---

*本文档为毕业设计论文提供技术支撑材料*
